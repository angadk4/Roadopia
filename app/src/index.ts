import type { Route } from '@shared/types';

/** Placeholder app entry (M0-T03 skeleton). The real client lands at SPK-01 / M7. */
export const APP_NAME = 'roadopia';

/** Stub proving the shared domain types resolve into the app (M0-T06 DoD). */
export type AppRoute = Route;
